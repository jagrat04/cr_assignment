[project]
name = "smartdialer"
version = "0.1.0"
description = "SmartDialer prototype: progressive + safety-gated predictive dialing"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
